# Public Assets

Place your background image here as `background.jpg`.

In the app, the login screen background uses `/background.jpg`.

If you want to use a different file name, update `src/App.tsx` backgroundUrl accordingly.
